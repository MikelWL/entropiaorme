{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "sale-capture-overlay",
  "description": "Least-privilege window access for the sale-window capture button.",
  "windows": ["overlay-sale-capture"],
  "permissions": [
    "sale-capture-commands",
    "core:window:allow-hide",
    "core:window:allow-set-size",
    "core:window:allow-start-dragging"
  ]
}
