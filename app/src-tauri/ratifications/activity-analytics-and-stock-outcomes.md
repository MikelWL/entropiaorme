# Activity analytics and stock outcomes golden ratification

An independent adversarial review examined every changed demo and corpus golden against the final typed analytics contract, tracker behaviour, stock conversion semantics, and deterministic fixture inputs.

The Hunting analytics demo pin matches the reduced profession-economic DTO. Its overall, definition, ambient activity, and species figures reconcile, including reward value and realised-rate arithmetic. The Harvest demo response remains the truthful empty result for the bundled demo database; its only textual change is removal of a trailing newline.

Across the corpus, each changed database snapshot removes exactly one legacy `Shrapnel Conversion` ledger row worth one per cent of ordinary Shrapnel TT. No unrelated domain value changes accompany those removals. A small number of normalised timestamp symbols shift only because the removed ledger timestamp no longer occupies a symbol. Dedicated tracker tests continue to prove that enhancer rebates are retained, ordinary Shrapnel remains active stock until an explicit conversion, activity attribution is provenance-aware, and transactional rollback is intact.

The typed end-to-end fixture was also checked for internal DTO coherence. It remains a user-interface contract fixture; accounting correctness is established by the reconciled demo golden and service-level tests.

```text
ORACLE-RATIFICATION
range: origin/main..HEAD
goldens: analytics_harvest, analytics_hunting_activity, basic_hunt_10_events, crit_dodge_evade_jam, enhancer_break_during_hunt, global_item_drop, global_kill_correlated, hof_item_drop, hof_kill_correlated, mission_completion_with_reward_suppression, multi_mob_hunt_loot_grouping, placeholder_recorded_hunt, single_mob_hunt, skill_gain_across_tick, tree_harvesting_session
VERDICT: ratification-sound
```
