# cargo-deny configuration: supply-chain policy for the Rust workspace
# (the Tauri shell and the native-backend members).
#
# Four check classes run in CI (`cargo deny check`):
#   - advisories: RUSTSEC advisories against the Windows-target dependency
#     graph. The ignore list is the subset of .cargo/audit.toml entries
#     that exist in that graph (cargo-audit scans the whole lockfile,
#     including Linux-only subtrees, so its list is the superset); the
#     per-advisory rationale lives there. Review both files together on
#     every Tauri bump.
#   - licenses: an explicit allowlist derived from the actual dependency
#     tree. A new dependency carrying a licence outside this list fails CI
#     and forces a deliberate decision rather than a silent acceptance.
#   - bans: duplicate-version reporting stays advisory (the Tauri tree
#     legitimately carries several); explicitly banned crates fail.
#   - sources: every crate must come from crates.io; git dependencies and
#     alternative registries fail until allowlisted here.

[graph]
# Resolve the policy against the targets we actually ship (Windows, and
# the Linux deb/AppImage) rather than the full cross-platform graph, so
# subtrees for platforms we do not build (macOS et al.) do not drive
# licensing decisions for artefacts we ship.
targets = ["x86_64-pc-windows-msvc", "x86_64-unknown-linux-gnu"]
all-features = true

[advisories]
version = 2
yanked = "deny"
# The shipped-graph subset of .cargo/audit.toml (single source of full
# rationale). Most are `unmaintained` warnings on transitive Tauri-toolchain
# crates: the gtk-rs GTK3 family is the Linux backend every Tauri Linux app
# carries until Tauri migrates off GTK3. The two quick-xml entries are DoS
# *vulnerability* advisories, suppressed only because a reachability audit
# proved their XML-parse path is dead code in the shipped artefacts; the
# `wrappers = ["plist"]` ban below pins that assumption structurally. Drop
# them when plist reaches quick-xml >= 0.41. Review both files together on
# every Tauri bump.
ignore = [
    "RUSTSEC-2024-0411", # gdkwayland-sys (gtk-rs GTK3 family, unmaintained)
    "RUSTSEC-2024-0412", # gdk
    "RUSTSEC-2024-0413", # atk
    "RUSTSEC-2024-0414", # gdkx11-sys
    "RUSTSEC-2024-0415", # gtk
    "RUSTSEC-2024-0416", # atk-sys
    "RUSTSEC-2024-0417", # gdkx11
    "RUSTSEC-2024-0418", # gdk-sys
    "RUSTSEC-2024-0419", # gtk3-macros
    "RUSTSEC-2024-0420", # gtk-sys
    "RUSTSEC-2024-0370", # proc-macro-error (build-time macro crate)
    "RUSTSEC-2025-0075", # unic-char-range (build-time Unicode data)
    "RUSTSEC-2025-0080", # unic-common
    "RUSTSEC-2025-0081", # unic-char-property
    "RUSTSEC-2025-0098", # unic-ucd-version
    "RUSTSEC-2025-0100", # unic-ucd-ident
    "RUSTSEC-2025-0057", # fxhash (no trust-bearing usage)
    "RUSTSEC-2026-0194", # quick-xml: quadratic duplicate-attribute check (DoS; see audit.toml)
    "RUSTSEC-2026-0195", # quick-xml: unbounded NsReader ns-alloc (DoS; API unused by plist)
]

[licenses]
version = 2
# Allowlist derived from the resolved Windows dependency tree. Additions
# require a deliberate edit here, reviewed like any other policy change.
allow = [
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "BSL-1.0",
    "CC0-1.0",
    "ISC",
    "MIT",
    "MIT-0",
    "MPL-2.0",
    "Unicode-3.0",
    "Zlib",
]
confidence-threshold = 0.8

[bans]
# The Tauri tree carries legitimate duplicate versions (e.g. windows-sys);
# keep duplicates advisory and reserve hard failure for explicit bans.
multiple-versions = "warn"
wildcards = "deny"
# Intra-workspace path dependencies (a backend member depending on a sibling,
# e.g. eo-services -> eo-wire) are version-less by construction: they are
# publish = false and resolve locally, never from a registry. Exempt path deps
# from the wildcard ban so it still bites a real registry crate pinned to "*"
# while the normal workspace shape is allowed.
allow-wildcard-paths = true

# quick-xml carries live DoS advisories (RUSTSEC-2026-0194 / -0195), suppressed
# in [advisories] above only because it is reachable solely as a transitive
# child of plist, whose XML-parse path is dead code in the Windows artefact. Pin
# that reachability assumption structurally: allow quick-xml ONLY under plist, so
# any future change introducing a new quick-xml path (a first-party XML parser, a
# different dependency) trips CI and forces the suppression to be revisited.
[[bans.deny]]
name = "quick-xml"
wrappers = ["plist"]

[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
